import { CategoryImageSharpPipe } from './category-image-sharp.pipe';

describe('CategoryImageSharpPipe', () => {
  it('should be defined', () => {
    expect(new CategoryImageSharpPipe()).toBeDefined();
  });
});
