import { ProfileImageSharpPipe } from './profile-image-sharp.pipe';

describe('ProfileImageSharpPipe', () => {
  it('should be defined', () => {
    expect(new ProfileImageSharpPipe()).toBeDefined();
  });
});
